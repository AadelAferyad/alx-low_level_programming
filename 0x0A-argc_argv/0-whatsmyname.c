#include <stdio.h>

/**
 * main - Entry point
 *
 * @argc: the argument count.
 * @argv: the argument vector.
 *
 * Return: Always 0 (Success).
 */

int main(int argc, char **argv)
{
	(void)argc;
	printf("%s\n", *argv);
	return (0);
}
